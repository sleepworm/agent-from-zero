# TODO — 待深入研究的问题

记录学习过程中发现、但当前 commit 范围内没时间/没必要深入的问题，留到合适的 Phase 再回头专门处理，不是随手就忘。

---

## Prompt Caching 命中门槛（源自 Commit 0.3）

`chat()` 的 `usage` 里能看到 `prompt_cache_hit_tokens`/`prompt_cache_miss_tokens` 的拆分，而且命中缓存的单价（$0.003/1M）比未命中（$0.15/1M）便宜了 50 倍——`_estimate_cost_usd()` 只是被动读取这两个数字去算钱，**这个缓存机制本身什么时候会命中，完全没有深入**。

Commit 0.3 连续发了 5 次真实请求，全部共享同一个 `DEFAULT_SYSTEM`，`cache_hit_tokens` 五次全部是 0——没有观察到任何一次命中，即使按直觉推测"重复的固定前缀应该有机会命中"。需要专门补一节，至少回答：

- 命中门槛是什么？是不是固定前缀太短（这一步的 `DEFAULT_SYSTEM` 只有一句话）根本不够格进缓存？
- 命中窗口多长？两次请求间隔多久会导致缓存失效——5 次请求之间的间隔（几秒到十几秒）是不是已经超过了窗口？
- 缓存匹配的粒度是整段 prompt 还是按 token 前缀比较？前缀相同、后缀不同的两次请求算不算命中一部分？
- 这和 OpenAI / Anthropic 各自的 prompt caching 机制是不是同一套原理，还是三家实现思路完全不同（失效策略、计费方式、是否需要显式声明"这段要被缓存"）？
- Phase 6（Context）会引入固定不变的业务上下文，Phase 7（Context Pruning）的裁剪策略会不会意外破坏本来能命中的缓存？这两个问题最好等做到那两章时一起看，而不是现在盲猜。

**处理方式还没定**：可能是 Phase 6 或 Phase 14（涉及成本）顺手加一段说明，也可能值得单独开一个 commit 专门讲清楚。先记在这里，做到那附近再决定。

---

（后续发现的其他待深入问题，继续往下加。）
